[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "odsp-occurrence-information"
version = "0.2.0"
description = "Source-preserving occurrence information layers for biodiversity records"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools.packages.find]
where = ["."]
include = ["odsp*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
